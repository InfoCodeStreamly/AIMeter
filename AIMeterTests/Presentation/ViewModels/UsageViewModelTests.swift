import Testing
@testable import AIMeter

@Suite("UsageViewModel", .tags(.presentation))
struct UsageViewModelTests {
    
    @Test("placeholder")
    func placeholder() {
        #expect(true)
    }
}
