import Testing
@testable import AIMeter

@Suite("SessionKeyRepository Protocol")
struct SessionKeyRepositoryTests {
    
    @Test("placeholder")
    func placeholder() {
        #expect(true)
    }
}
